<?xml version="1.0" encoding="utf-8"?>
<!--
  Cleartext (HTTP) traffic is blocked by default on modern Android. It is
  permitted here ONLY for local development hosts:
    - 10.0.2.2  : the host machine's localhost as seen from the Android emulator
    - localhost / 127.0.0.1 : loopback
  All other traffic (including the deployed Render backend, which uses HTTPS)
  keeps the secure default of no cleartext.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
